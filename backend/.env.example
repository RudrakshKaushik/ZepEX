# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,zepex.onrender.com

# Comma-separated full URLs (include https://). Example for production:
# DJANGO_CORS_ALLOWED_ORIGINS=https://zepex.vercel.app
# DJANGO_CSRF_TRUSTED_ORIGINS=https://zepex.vercel.app
DJANGO_CORS_ALLOWED_ORIGINS=
DJANGO_CSRF_TRUSTED_ORIGINS=
DATABASE_URL=postgresql://postgres:demo@127.0.0.1:5432/demo
REDIS_URL=redis://localhost:6379/0

# Required for receipt AI extraction (Google AI Studio)
GEMINI_API_KEY=

# Set False in production when running Celery worker + Redis
CELERY_TASK_ALWAYS_EAGER=True

# Email (Django reads EMAIL_*; SMTP_* aliases are also supported)
# NOTE: backend/.env is local only — copy these to Render/Vercel env dashboards for production.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-gmail-app-password
DEFAULT_FROM_EMAIL=your@gmail.com
EMAIL_USE_TLS=True

# Exchange rate API (used for multi-currency reimbursement)
EXCHANGE_RATE_PROVIDER=ExchangeRate API
EXCHANGE_RATE_API_URL=https://v6.exchangerate-api.com/v6
EXCHANGE_RATE_API_KEY=
